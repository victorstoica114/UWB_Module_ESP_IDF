# Reference Material

This folder is for legacy files and migration notes that are useful to keep,
but are not part of the active ESP-IDF build.

Active firmware code should live in `main/` or `components/`.
