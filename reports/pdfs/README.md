# Final PDF reports

This directory contains one uniquely named final PDF for each retained report
bundle. Filenames match the source report-directory names. Supporting plot
PDFs remain with their source reports.

Use `SHA256SUMS` to verify the 13 files.
