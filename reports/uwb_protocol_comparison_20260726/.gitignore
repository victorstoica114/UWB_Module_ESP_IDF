# Keep the canonical field captures in Git as lossless XZ archives.
data/*.jsonl

# Preliminary five-second collector smoke tests are local diagnostics only.
smoke/

# Reproducible build/runtime by-products.
analysis_run.log
report.pdf
report.aux
report.fdb_latexmk
report.fls
report.log
report.out
report.toc
